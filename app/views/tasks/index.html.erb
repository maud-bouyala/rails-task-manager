<h1>My tasks</h1>
<ul>
  <% @tasks.each do |task| %>

    <li><%= link_to task.title, task_path(task) %>
     <%= check_box(:completed,{:id => task.completed,:value => task.completed}) %></li>
  

  <% end %>
</ul>

<a href="/new">Add a new task</a>
